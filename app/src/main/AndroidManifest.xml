<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.audioacquisition">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- 录音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" /> <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 播音 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Practice.activity.VideoActivity">
        </activity>
        <activity android:name=".Practice.activity.TypicalActivity" />
        <activity android:name=".Practice.activity.TeachActivity" />
        <activity android:name=".Practice.activity.OtherActivity" />
        <activity android:name=".Practice.activity.PolicyActivity" />
        <activity android:name=".Home.activity.KindActivity" />
        <activity android:name=".Practice.activity.FaxActivity" />
        <activity android:name=".Mine.activity.StudyActivity" />
        <activity android:name=".Mine.activity.PracticeActivity" />
        <activity android:name=".Mine.activity.CourceActivity" />
        <activity android:name=".Core.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Practice.activity.TestActivity" />
        <activity android:name=".Home.activity.HorizontalActivity" />
        <activity android:name=".Mine.activity.LoginActivity" />
        <activity android:name=".Mine.activity.ChangeActivity" />
        <activity android:name=".Core.activity.MainActivity" />
    </application>

</manifest>